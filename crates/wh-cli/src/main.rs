fn main() {
    println!("wh — wheelhouse cli");

    // Error rendering infrastructure is available in wh_cli::output for use
    // by future command handlers. See output/error.rs for WhError hierarchy
    // and output/mod.rs for render_error() and OutputFormat.
}
